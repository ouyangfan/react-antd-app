import React, { useEffect, useState } from "react";
import { Card } from "antd";
const OrderService: React.FC = () => {
  return (
    <Card style={{ width: "100%", marginTop: "20px" }}>
      <p className="card-title">行前必读</p>
    </Card>
  );
};

export default OrderService